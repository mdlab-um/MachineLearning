Assignment:
1. define your own CNN architecture using pytorch
2. train the CNN model on Chinese character dataset (`Chinese-mnist` from the `kagglehub` python package)
3. report training, validation and testing accuracy and confusion matrices
